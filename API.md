# REST API — ระบบพระโอวาท ver2

ฐาน: `/api/v1`
เวอร์ชันอยู่ใน path เพื่อให้ LINE OA และแอปในอนาคตยึดสัญญานี้ได้ ต่อให้เว็บเปลี่ยนหน้าตา

## รูปแบบการตอบกลับ

ทุกปลายทางตอบรูปแบบเดียวกัน ฝั่งรับเขียนโค้ดจัดการแบบเดียวได้ทั้งระบบ

```json
{ "data": { }, "meta": { }, "error": null }
```

สำเร็จ → `data` มีค่า, `error` ไม่มี
ผิดพลาด → `data` เป็น `null`, `error` มี `code` กับ `message`

```json
{ "data": null, "error": { "code": "PENDING_APPROVAL", "message": "บัญชีของคุณอยู่ระหว่างรอผู้ดูแลระบบอนุมัติ" } }
```

`message` เป็นภาษาไทยที่แสดงให้ผู้ใช้อ่านได้ทันที ไม่ต้องแปลเพิ่ม
`code` ใช้ตัดสินใจในโค้ด (อย่าตัดสินใจจากข้อความ เพราะข้อความแก้ได้)

| code | HTTP | ความหมาย |
|---|---|---|
| `VALIDATION_ERROR` | 400 | ข้อมูลที่ส่งมาไม่ครบหรือผิดรูปแบบ |
| `UNAUTHENTICATED` | 401 | ยังไม่เข้าสู่ระบบ หรืออีเมล/รหัสผ่านไม่ถูกต้อง |
| `FORBIDDEN` | 403 | เข้าสู่ระบบแล้วแต่ไม่มีสิทธิ์ |
| `PENDING_APPROVAL` | 403 | สมัครแล้วแต่ผู้ดูแลยังไม่อนุมัติ |
| `ACCOUNT_BLOCKED` | 403 | บัญชีถูกระงับหรือไม่ได้รับอนุมัติ |
| `NOT_FOUND` | 404 | ไม่พบสิ่งที่ขอ |
| `DUPLICATE` | 409 | ซ้ำกับที่มีอยู่แล้ว (เช่น อีเมลสมัครไปแล้ว) |
| `DAILY_LIMIT_REACHED` | 429 | เกินโควตาของวัน |
| `INTERNAL_ERROR` | 500 | ระบบขัดข้อง |

---

## พระโอวาท

### `GET /api/v1/teachings`
ค้นหา + ฟิลเตอร์ + แบ่งหน้า (หน้าละ 50)

| พารามิเตอร์ | ความหมาย |
|---|---|
| `q` | คำค้น เว้นว่าง = เปิดดูทั้งหมด |
| `deity` | องค์ผู้ประทาน |
| `temple` | สถานธรรม |
| `category` | ชั้นเรียน |
| `year` | ปี พ.ศ. |
| `page` | หน้าที่ต้องการ เริ่มที่ 1 |

`data.hits[]` แต่ละรายการมี `teachingId`, `snippet`, `deity`, `temple`, `province`, `date`, `category`, `matchedTerms`, `matchedField`
`data.facets` คือตัวเลือกฟิลเตอร์พร้อมจำนวน — จำนวนของแต่ละมิติคิดโดย**ข้ามฟิลเตอร์ของมิตินั้นเอง** ผู้ใช้จึงเปลี่ยนตัวเลือกในมิติเดิมได้
`meta` มี `page`, `pageSize`, `total`, `totalPages`

### `GET /api/v1/teachings/facets`
ตัวเลือกฟิลเตอร์อย่างเดียว รับพารามิเตอร์ชุดเดียวกับข้างบน (ไม่มี `page`)
ใช้ตอนเปิดหน้าค้นหาโดยยังไม่ค้นอะไร

### `GET /api/v1/teachings/:id`
อ่านฉบับเต็ม รับได้ทั้ง id ใหม่ (uuid) และ id เดิมจาก ver1 — ลิงก์เก่าที่ส่งต่อกันไว้ยังเปิดได้

> `content` ที่ตอบกลับคือ**ต้นฉบับทุกตัวอักษร** ไม่มีจุดใดในระบบที่แก้ ตัด หรือเรียบเรียงใหม่

---

## เปิดรับพระโอวาทประจำวัน

### `POST /api/v1/draws/daily`
```json
{ "question": "ทำอย่างไรกับความท้อแท้", "guestKey": "<รหัสเครื่อง>" }
```
`question` เว้นว่างได้ = สุ่มล้วน
`guestKey` จำเป็นเมื่อยังไม่เข้าสู่ระบบ — เข้าสู่ระบบแล้วผูกกับบัญชีให้เอง

**เปิดรับได้วันละครั้ง บังคับที่ฐานข้อมูล** เรียกซ้ำในวันเดียวกันจะได้ท่อนเดิมกลับไป
พร้อม `meta.alreadyDrawnToday: true` (ไม่ใช่ข้อผิดพลาด)

### `GET /api/v1/draws/daily`
ดูโควตาที่เหลือของวันนี้ → `data.remaining` (0 หรือ 1)

---

## สมาชิก

| ปลายทาง | ทำอะไร |
|---|---|
| `POST /api/v1/auth/register` | สมัครสมาชิก — สถานะเริ่มต้น "รอตรวจสอบ" ยังเข้าใช้งานไม่ได้ |
| `POST /api/v1/auth/login` | เข้าสู่ระบบ ตั้งคุกกี้ httpOnly ให้ |
| `POST /api/v1/auth/logout` | ออกจากระบบ |
| `GET /api/v1/auth/me` | ผู้ใช้ที่กำลังเข้าใช้งาน — ยังไม่เข้าสู่ระบบตอบ **200 พร้อม `user: null`** ไม่ใช่ 401 |

สมัครสมาชิกส่ง `name`, `dharmaTitle`, `templeName`, `email`, `password`, `confirmPassword`

รหัสผ่านเก็บด้วย **bcrypt** (ver1 ใช้ sha256 ไม่มี salt)
สถานะการเข้าใช้งานอยู่ในคุกกี้ httpOnly ชื่อ `ow_session` — หน้าเว็บไม่ต้องเก็บ token เอง

---

## ผู้ดูแลระบบ

### `GET /api/v1/admin/members`
รายชื่อสมาชิก รายที่รอตรวจสอบขึ้นก่อน กรองด้วย `?status=pending|active|rejected|blocked`

### `PATCH /api/v1/admin/members`
```json
{ "id": "<id สมาชิก>", "status": "active" }
```
ผู้ดูแลเปลี่ยนสถานะบัญชีตัวเองไม่ได้ — กันเผลอตัดสิทธิ์ตัวเองจนไม่มีใครเข้าหลังบ้านได้

---

## โครงสร้างโค้ดที่อยู่เบื้องหลัง

```
api/v1/[...path].ts   ทางเข้าเดียว ไม่มีตรรกะ
src/api/v1/router.ts  ตารางเส้นทางทั้งหมด
src/api/v1/routes/    รับ request → เรียก service → ตอบกลับ (บางที่สุด)
src/services/         ตรรกะงานและกฎธุรกิจ
src/repositories/     ที่เดียวที่คุยกับฐานข้อมูล
src/domain/           โครงสร้างข้อมูลและกฎที่ไม่ขึ้นกับฐานข้อมูลหรือ HTTP
src/shared/           รูปแบบตอบกลับ, ค่าตั้งต้น, ตัวช่วย HTTP, ตัวเชื่อมฐานข้อมูล
```

กฎ: `api` เรียก `services` เท่านั้น · `services` เรียก `repositories` เท่านั้น · **ห้ามข้ามชั้น**
เปลี่ยนวิธีเก็บข้อมูลภายหลัง แก้แค่ชั้น `repositories` ชั้นเดียว

รวมเป็นฟังก์ชันเดียวเพราะแพ็กเกจ Vercel จำกัดจำนวนฟังก์ชันต่อโปรเจกต์
(ver1 ใช้ไปแล้ว 7 ถ้า ver2 แยกอีก 9 จะเกินจน deploy ไม่ผ่าน)

---

## ตรวจว่าผลค้นหายังตรงกับ ver1

```bash
npm run check:search
```

ป้อนข้อมูลชุดเดียวกันให้ทั้งสองเวอร์ชันแล้วเทียบจำนวน ลำดับ และตัวเลือกฟิลเตอร์
ต้องผ่านทุกคำค้นก่อน deploy เสมอ — ถ้าไม่ตรง แปลว่ามีพระโอวาทหายหรือเรียงผิด
