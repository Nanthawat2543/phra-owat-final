# กฎสำคัญของโปรเจกต์พระโอวาท

## ⛔ ห้ามแก้ข้อความพระโอวาทเด็ดขาด

**เนื้อหาพระโอวาททุกตัวอักษรต้องตรงกับต้นฉบับที่ได้รับมา 100%**

ห้ามเพิ่มเติม แต่ง ตัด สรุป เรียบเรียงใหม่ หรือ "ปรับให้อ่านง่ายขึ้น" ไม่ว่ากรณีใด รวมถึง:

- **คำภาษาอังกฤษ/ศัพท์สมัยใหม่ที่ปนอยู่ในเนื้อหา** — เช่น `Unlimited Keywords`, `Skillset`,
  `Core value`, `Growth`, `Reflection` ในพระโอวาทเต้าจี้เทียนจุน (23 ส.ค. 2569)
  คำเหล่านี้อยู่ในต้นฉบับจริง ถึงจะดูแปลกก็ต้องเก็บไว้
- **คำที่ดู "ผิด" หรือสะกดแปลก** — ห้ามแก้ตามที่คิดว่าถูก
- **การจัดย่อหน้า ลำดับข้อความ การเว้นวรรค**

**เหตุผล:** เป็นคำสอนศักดิ์สิทธิ์ที่สิ่งศักดิ์สิทธิ์ประทานไว้ การเปลี่ยนแม้คำเดียวคือการบิดเบือนคำสอน

### ข้อยกเว้นเดียว — ความเพี้ยนจากเครื่องมือแปลงไฟล์

ถ้าแปลง PDF/Word แล้วตัวอักษรเพี้ยน (ไม่ใช่ต้นฉบับผิด) เช่น
- สระ ำ หายกลายเป็น า → `บำเพ็ญ` กลายเป็น `บาเพ็ญ`, `ทำ` → `ทา`, `จำกัด` → `จากัด`
- มีช่องว่างแทรกกลางคำ → `เป็ น`, `ล้ วน`

ต้อง **เปิดดูภาพ PDF ต้นฉบับทีละหน้า แล้วพิมพ์ให้ตรงภาพ** (กู้คืนให้ตรงต้นฉบับ ไม่ใช่เดาเอง)
ใช้ `pdftotext -raw` ช่วยได้ แต่ต้องเทียบกับภาพเสมอ

### ถ้าสงสัย — ถามก่อนเสมอ

เห็นข้อความที่คิดว่าควรกรอง/ตัดออก (หัวข้อกำกับ คำซ้ำ ฯลฯ) **ห้ามตัดสินใจเอง** ให้ถามผู้ดูแลก่อน

> หมายเหตุ: การกรองข้อความใน `api/_lib/passages.js` (`skipPatterns` / `isJunkPassage`)
> มีผลแค่ตอน *เลือกท่อนมาแสดง* ในการค้นหา/สุ่มเท่านั้น — ห้ามใช้แก้หรือลบเนื้อหาที่เก็บในคลัง

## 🎨 ห้ามแก้การจัดหน้า/ฟอนต์

อาจารย์กำหนดให้รูปแบบตัวอักษร การจัดวาง และเลย์เอาต์ตรงกับต้นฉบับ — ห้ามเปลี่ยนขนาดฟอนต์
ตระกูลฟอนต์ การจัดกึ่งกลาง ระยะบรรทัด หรือตำแหน่งข้อความ โดยไม่ได้รับอนุมัติ

## การเพิ่มพระโอวาทใหม่

```bash
node scripts/add-teaching.mjs --file เนื้อหา.txt --deity "ชื่อองค์" \
  --temple "สถานธรรม" --province "จังหวัด" --date 2026-08-23 \
  --category "ชั้นเรียน" --dry     # ใส่ --dry เพื่อดูก่อนบันทึก
```

สคริปต์กัน id/เนื้อหาซ้ำ และสำรองไฟล์เดิมให้อัตโนมัติ หลังเพิ่มต้อง `npm run build`
แล้ว commit + deploy ข้อมูลจึงจะขึ้นเว็บจริง
